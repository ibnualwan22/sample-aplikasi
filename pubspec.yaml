name: sample_aplikasi
description: "Aplikasi SIGMA Mobile"
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  http: ^1.2.1
  intl: ^0.19.0
  adhan: ^2.0.0
  geolocator: ^13.0.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  url_launcher: ^6.3.2
  table_calendar: ^3.1.3
  webview_flutter: ^4.13.1
  shorebird_code_push: ^2.0.5
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  shared_preferences: ^2.3.2
  flutter_dotenv: ^5.2.1
  flutter_qiblah: ^3.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - .env
    - shorebird.yaml
    - assets/quran_index.json
  uses-material-design: true
